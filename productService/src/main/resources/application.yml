server:
  port: 9001

spring:
  application:
    name: PRODUCT-SERVICE

eureka:
  instance:
    preferIpAddress: true
    hostname: eureka-0
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_ADDRESS}