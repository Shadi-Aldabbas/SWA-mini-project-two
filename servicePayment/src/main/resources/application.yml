server:
  port: 9003

spring:
  application:
    name: PAYMENT-SERVICE

eureka:
  instance:
    preferIpAddress: true
    hostname: eureka-0
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_ADDRESS}