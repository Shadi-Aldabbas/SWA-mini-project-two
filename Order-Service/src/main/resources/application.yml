server:
  port: 9002

spring:
  application:
    name: ORDER-SERVICE

eureka:
  instance:
    preferIpAddress: true
    hostname: eureka-0
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_ADDRESS}